{
  "name": "@libre-ai/contracts-authority",
  "version": "0.0.0",
  "private": true,
  "license": "EUPL-1.2",
  "type": "module",
  "packageManager": "bun@1.4.0-canary.1",
  "engines": {
    "bun": ">=1.4.0"
  },
  "overrides": {
    "fast-uri": "4.1.4"
  },
  "dependencies": {
    "@noble/curves": "2.4.0",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "entities": "8.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2",
    "@libre-ai/governance": "file:../project-governance"
  },
  "scripts": {
    "check:auth-retention": "bun run check:bun:runtime && bun test --config=tools/quality/auth-retention-v3.bunfig.toml tools/quality/auth-retention-v3.test.ts",
    "check:bun:runtime": "bun node_modules/@libre-ai/governance/tools/quality/check-bun-minimum.ts",
    "check:bun": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-bun-manifests.ts",
    "check:toolchain": "bun run check:bun && bun node_modules/@libre-ai/governance/tools/quality/check-toolchain.ts",
    "check:contracts": "bun run check:bun:runtime && bun tools/quality/check-contracts.ts && bun run check:build-brief",
    "check:secret-scan": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-secret-scan.ts",
    "check:personal-data": "bun run check:bun:runtime && bun node_modules/@libre-ai/governance/tools/quality/check-personal-data-boundary.ts",
    "lint": "bun run check:bun:runtime && biome ci .",
    "typecheck": "bun run check:bun:runtime && tsc --noEmit -p tsconfig.json",
    "pretest": "bun run check:bun",
    "test": "bun test tools/quality",
    "check": "bun run check:bun && bun run check:auth-retention && bun run check:toolchain && bun run check:contracts && bun run check:secret-scan && bun run check:personal-data && bun run lint && bun run typecheck && bun run test && bun run check:packages",
    "check:build-brief": "bun run check:bun:runtime && bun test --config=tools/quality/build-brief-v2.bunfig.toml tools/quality/build-brief-v2.test.ts",
    "check:build-brief:crypto-oracle": "bun run check:bun:runtime && cargo run --locked --manifest-path tools/quality/build-brief-crypto-oracle/Cargo.toml -- contracts/fixtures/build-brief-v2/strict-ed25519-vectors.json",
    "check:packages": "bun run check:bun:runtime && bun run --cwd packages/sdk-ts check && bun run --cwd packages/envelope check && bun run --cwd crates/sdk-rs check && cargo test --locked --manifest-path crates/sdk-rs/Cargo.toml"
  },
  "workspaces": [
    "packages/*",
    "crates/sdk-rs"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/libre-ai/schemas-and-contracts.git"
  }
}
